/**
 * @deprecated Use `getCanvasStorage()` from `../canvas-storage` instead.
 * Mantido apenas para compatibilidade com scripts locais.
 */
export { sqliteCanvasStorage } from '../canvas-storage/sqlite';
